name: COMP3104_Group74_Assignment Repository CI

run-name: ${{ github.actor }} - ${{ github.event.head_commit.message }}

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - run: echo Success